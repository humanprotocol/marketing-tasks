export enum SubmissionStatus {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
  FAILED = 'failed',
}

export enum VerificationResult {
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
}
